<template>
    <lightning-card title="Loan Request Form" icon-name="custom:custom63">
        <div class="slds-p-around_medium">
            <lightning-input
                type="text"
                label="Customer Name"
                value={customerName}
                onchange={handleCustomerNameChange}
                required>
            </lightning-input>

            <lightning-input
                type="number"
                label="Loan Amount"
                value={loanAmount}
                onchange={handleLoanAmountChange}
                required>
            </lightning-input>

            <lightning-combobox
                name="loanStatus"
                label="Loan Status"
                value={loanStatus}
                options={statusOptions}
                onchange={handleLoanStatusChange}
                required>
            </lightning-combobox>

            <div class="slds-m-top_medium slds-grid slds-grid_vertical-align-center">
                <lightning-button
                    variant="brand"
                    label="Save"
                    onclick={handleSave}
                    disabled={isSaving}>
                </lightning-button>
                <template if:true={isSaving}>
                    <lightning-spinner alternative-text="Saving..." size="small" class="slds-m-left_small"></lightning-spinner>
                </template>
            </div>

            <template if:true={errorMessage}>
                <div class="slds-m-top_small slds-text-color_error">{errorMessage}</div>
            </template>

            <template if:true={successMessage}>
                <div class="slds-m-top_small slds-text-color_success">{successMessage}</div>
            </template>
        </div>
    </lightning-card>
</template>
